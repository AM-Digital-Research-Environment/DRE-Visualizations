<?php
/**
 * Podcasts page block.
 *
 * Analytics for the cluster's curated podcast episodes (profile key itemSets.podcasts,
 * template 21, fabio:AudioDocument). Loads the precomputed podcasts.json and
 * renders via the standard dashboard orchestrator (resourceType 'podcasts' →
 * the podcasts layout). The headline chart is a word cloud built in the
 * precompute from the episodes' AI-generated transcripts (bibo:content).
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */
?>
<?= $this->partial('common/block-layout/partials/dashboard-async', [
    'itemId'  => 'podcasts',
    'slug'    => 'podcasts',
    'loading' => $this->translate('Loading podcast analytics…'),
]) ?>
